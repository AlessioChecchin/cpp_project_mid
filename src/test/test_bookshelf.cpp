#include "test/test_bookshelf.h"

#include "test/test.h"

namespace test
{

void test_bookshelf_constructor()
{
	/*prj::my_vector a;
	check(a.size() == 0);
	check(a.capacity() == 0);

	constexpr int size = 10;

	prj::my_vector b(size);
	check(b.size() == size);
	check(b.capacity() == size);

	prj::my_vector c(b);
	check(c.size() == size);
	check(c.capacity() == size);

	prj::my_vector d(c);*/
}

void test_bookshelf_push_back()
{
	/*prj::my_vector a;

	constexpr int size = 100;

	for(int i = 0; i < size; i++)
	{
		a.push_back(i + 1);
	}

	for(int i = 0; i < size; i++)
	{
		check(a[i] == i + 1);
		check(a.at(i) == i + 1);
	}

	a.pop_back();

	check(a[a.size() - 1] == size - 1);
	check(a.size() == size - 1);*/
}


} // test
